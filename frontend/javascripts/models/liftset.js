class LiftSet {
  constructor(reps, weight, goal){
      this.reps = reps;
      this.weight = weight;
      this.goal = goal;
  }

  generateForm() {
    alert('hit LiftSet.generateForm method');
  }

}